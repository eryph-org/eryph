<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Pomelo is unmaintained and has no EF Core 10 release; use the maintained fork. -->
    <PackageReference Include="Microting.EntityFrameworkCore.MySql" Version="10.0.8" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Eryph.StateDb\Eryph.StateDb.csproj" />
  </ItemGroup>

</Project>
